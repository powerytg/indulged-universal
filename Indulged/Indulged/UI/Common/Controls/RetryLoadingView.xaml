<local:LoadingViewBase
    x:Class="Indulged.UI.Common.Controls.RetryLoadingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Indulged.UI.Common.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel Orientation="Vertical">
        <TextBlock x:Name="LoadingLabel" Text="Loading..."
                   Foreground="White" FontSize="18"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   TextWrapping="Wrap"
                   Margin="25"/>

        <ProgressRing x:Name="LoadingIndicator" 
                      HorizontalAlignment="Center" VerticalAlignment="Center"
                      Margin="0, 0, 0, 25"
                      IsActive="True" Background="Transparent"/>
        
        <!--Retry button-->
        <Button x:Name="RetryButton" Content="Retry"
                Style="{StaticResource MainButtonStyle}" HorizontalAlignment="Center"
                Click="RetryButton_Click"
                Visibility="Collapsed"/>
    </StackPanel>

</local:LoadingViewBase>
