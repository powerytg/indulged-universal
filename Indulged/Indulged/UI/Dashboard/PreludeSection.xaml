<UserControl
    x:Class="Indulged.UI.Dashboard.PreludeSection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Indulged.UI.Dashboard"
    xmlns:stream="using:Indulged.UI.Common.PhotoStream"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--Stream selection view-->
        <local:StreamSelectorView x:Name="SelectorView"
                                  VerticalAlignment="Top" HorizontalAlignment="Center"
                                  Grid.Row="0" Margin="0, 12, 0, 12"/>

        <!--Photo stream-->
        <stream:CommonStreamListView x:Name="PhotoListView"
                                      Grid.Row="1"/>

    </Grid>
</UserControl>
